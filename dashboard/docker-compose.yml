version: "3.8"

services:
  nextjs:
    container_name: sweetq
    build: .
    ports:
      - "3000:3000"
    restart: always

    environment:
      - IS_RUNTIME=true
      - NODE_OPTIONS=--max-old-space-size=512

    extra_hosts:
      - "srv659934.hstgr.cloud:46.202.168.61"

    volumes:
      - ./logs:/var/log/nextjs

    deploy:
      resources:
        limits:
          memory: 1G
