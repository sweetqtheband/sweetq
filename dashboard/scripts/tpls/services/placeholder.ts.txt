import axios from "./_db";
import { BaseList } from "./_list";
import { FIELD_TYPES } from "../constants";
import { onDelete, onSave } from "./_methods";

// TODO: Personaliza los tipos de campo según tu entidad
export const Types = {
  _id: FIELD_TYPES.HIDDEN,
  // name: FIELD_TYPES.TEXT, // Ejemplo de campo
};

export const Options = {};

// TODO: Personaliza los títulos de campo según tu entidad
const fields = {
  titles: {
    // name: 'fields.name', // Ejemplo
  },
  types: Types,
  options: Options,
};

let options: Record<string, any>[] = [];

// Get options
const getOptions = async (params: any = {}) => {
  params.limit = 10000;
  return {
    options: [
      ...options,
      ...(await {{entity}}.getAll(params, true)).items.map(
        (item: Record<string, string>) => ({
          id: item._id,
          value: item.name, // TODO: Cambia 'name' por el campo que desees mostrar
        })
      ),
    ],
  };
};

const client = axios.create({
  baseURL: `${process.env.NEXT_PUBLIC_API_URI}/{{plural}}`,
});

export const {{entity}} = {
  ...BaseList(client),
  fields,
  getOptions,
};
