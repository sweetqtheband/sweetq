import { {{entity}} } from '@/app/models/{{camelCasePlural}}';
import { BaseSvc } from './_base';
import { Collection, Document } from 'mongodb';

/**
 * {{entity}} service
 */
export const {{entity}}Svc = (collection: Collection<Document>) => ({
  ...BaseSvc(collection, {{entity}}),
});
